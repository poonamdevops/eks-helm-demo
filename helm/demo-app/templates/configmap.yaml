apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "demo-app.fullname" . }}-config
  labels:
    {{- include "demo-app.labels" . | nindent 4 }}

data:
  APPLICATION_NAME: "eks-helm-demo"
  ENVIRONMENT: "{{ .Values.environment }}"
